# 빌드 산출물·로컬 전용 (builder가 컨테이너 안에서 새로 생성)
node_modules
.next
generated

# 시크릿
.env
.env*.local

# 로컬에서 업로드한 첨부파일 (개발용 실제 파일이 이미지에 섞여 배포되는 것을 막는다).
# 런타임 폴더는 Dockerfile의 mkdir로 생성되고, 실제 파일은 볼륨에 보관된다.
public/uploads

# 메타·문서·테스트 (이미지에 불필요)
.git
.gitignore
.github
.claude
*.md
docs
tests
playwright-report
test-results
.vscode

# 앱과 무관한 로컬 작업물.
# prototype은 별도 실험 프로젝트라 경로 별칭(@/…)이 이 앱에서 해석되지 않아
# 포함되면 next build의 타입체크가 실패한다. (git 미추적이라 CI에서는 드러나지 않음)
prototype
design-assets

# Docker 자체
Dockerfile
.dockerignore
docker-compose*.yml
