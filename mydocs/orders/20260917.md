---
kind: snapshot
status: active
canonical: mydocs/orders/20260917.md
last_verified: 2026-09-17
---

# 2026-09-17 작업

| 상태 | 내용 |
|---|---|
| 확인 | 업무 스레드에 족자 파일을 붙여 「완성했습니다」 를 보내면 오류 — 파일이 4MB(Vercel 4.5MB) 를 넘어 요청이 함수에 닿기 전에 끊긴다. 메시지도 저장되지 않았다. `troubleshootings/large-file-upload.md` |
| 완료 | **1단계: 상한을 화면이 먼저 말한다** — 고를 때 4MB 초과를 거르고 이유를 띄운다 · 채팅 패널이 업로드 실패를 건너뛰고 글만 보내던 것을 멈춘다. `fix/upload-size-limit` · `working/2026-09-17-upload-size-limit.md` |
| 결정 | 2단계는 **후보 A — NAS 에 있는 파일은 경로로 잇는다.** NAS 에 없는 파일은 올리고, 커서 막히면 NAS 에 올린 뒤 연결하라고 안내 |
| 완료 | **2단계: NAS 파일 연결** — + 메뉴 · 막힘 알림의 버튼 → 경로 붙여넣기/폴더 탐색 → 올리지 않고 첨부. 로컬 실측 185MB `.ai` 연결 → 메시지 → 링크 열기. `plans/2026-09-17-nas-file-link.md` · `working/2026-09-17-nas-file-link.md` |
| 고침 | 기존 NAS 탐색기 버그 3개 — 크기 안 보임(`lp1:` 접두사) · `&` 든 폴더 못 엶(`&amp;`) · 맥이 만든 NFD 파일명 404 |
| 완료 | **3단계: 이런 실패를 메일 + GitHub 이슈로** — 첨부 상한 초과 · 업로드 실패 · 전송 실패. 같은 오류는 열린 이슈에 댓글. 저장소가 공개라 이슈엔 이름 · 파일명 · 경로를 넣지 않는다 |
| 완료 | `GITHUB_ISSUE_TOKEN` 운영 환경변수(작업지시자) · PR #35 머지 · 운영 배포 |
| 확인 | 운영 시험 보고 → 이슈 #37 열림, 파일명 · UUID · 쿼리 0건 → 닫음 |
| 확인 | 운영에서 185MB `족자_2.ai` 내려받기 — 194,504,993B 전부 · 124.9초 · 1.6MB/s |
| 확인 | 알림 메일 — Resend **Delivered**. Gmail MCP 검색에는 보이지 않아 받은편지함 직접 확인 필요 |
| 고침 | 운영 `/api/nas` 로 NFD 이름 파일(`족자.ai`)을 NFC 경로로 열면 404 — `fix/nas-nfd-open`. 고치기 전 ✗ 404 → 고친 뒤 ✓ 200 (검증 43건) |
