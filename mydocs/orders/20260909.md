---
kind: snapshot
status: active
canonical: AGENTS.md
last_verified: 2026-09-09
---

# 2026-09-09

| 상태 | 일 |
|---|---|
| 완료 | 지식 카드가 왜 수동적인지 분석 — 카드는 문서(JSON 섹션)라 셀 수 없고, AI 는 askOmnis 한 문으로 벡터 top-8 만 읽으며, 채팅 쪽 AI(structureTask)는 카드를 안 읽고, 입력도 전부 수동. 대시보드가 카드 제목으로 `content.text` 를 읽는 결합도 발견 |
| 완료 | askOmnis 비용 구조 — 검색 8개 + 업무 593건·IP·CRM 전량 요약을 매번 넣어 질문당 15~30원(추정). 절반은 RAG 가 아니라 컨텍스트 채우기 |
| 결정 | 벤더를 먼저 정하지 않고 모델 × 방법론 그리드로 비용·시간·품질을 잰다 — `mydocs/plans/2026-09-09-model-method-benchmark.md`. 테스트 비용은 작업지시자 개인 부담, 실사용 검증 후 회사 청구 |
| 완료 | `bench/` 하네스 — Gemini·Anthropic·OpenAI 호환 어댑터, baseline·vector·hybrid·stuff·route 5 방법, 심판(gemini-2.5-pro), 재개 가능한 결과 파일, summary.md/csv. `npm run bench` |
| 완료 | `lib/ai.ts` 에서 옴니스 답변 프롬프트를 `buildOmnisAnswerPrompt` 로 뽑음 — 벤치와 프로덕션이 같은 문장을 쓴다. 동작 변화 없음 |
| 완료 | 전체 그리드 실행 `bench/results/20260909-full/` — Gemini/Gemma 9 모델 × 5 방법 × 10 질문 = 450 행, 11,080원(답변 4,056 + 심판 7,025). 집계표와 관찰은 `mydocs/working/2026-09-09-benchmark-round1.md`. 최고점 3.8-flash@low·route(만점·4.9초·8.3원), 가성비 3.8-flash@low·hybrid(1.8·2.0초·2.5원), 현행 2.5-flash·baseline 은 1.6·7.9초·15.8원 |
| 발견 | Gemma 무료 티어는 분당 토큰 한도라 baseline(3만 자)·stuff 는 못 돌린다. 2만 자 넘는 프롬프트는 보내지 않고 실패로 적는다 |
| 발견 | `Date.now()` 가 NTP 보정으로 뒤로 가서 음수 시간이 났다. 벤치는 `performance.now()` 로 |
| 발견 | 작업 트리에 이 세션이 만들지 않은 변경 24개 경로가 있다(CRM 재고·대시보드·설정·project-merge 이동 등). 다른 세션의 작업으로 보인다. lint 를 위해 `git stash`/`pop` 을 한 번 했고 충돌 없이 돌아왔다 |
| 부분 | Claude 추가 (9-10 충전 후 재개) — Haiku 4.5 5 방법 전부, Sonnet 5 4 방법 완료. Sonnet hybrid 1.8 · 17.8원 (Gemini 3.8@low hybrid 와 같은 점수, 7배 값). Sonnet route 는 3문항째에 다시 잔액 소진(stuff 가 5,460원) → 충전 후 완료: 1.67 · 7.7초 · 27원, A9 는 6단계 초과. Claude 총 11,990원. 잔액 소진 시 모델 건너뛰기 · 재시도 행 중복 제거(report dedupe) 추가 |
| 미착수 | 지원사업·수상·학회 엑셀(★ 지원사업 및 경진대회 수상, 학회_260827.xlsx: 지원사업 24 · 수상 9 · 부스 14)을 모델로 올리기 — 그 뒤 벤치 질문에 집계 문항 추가 |
