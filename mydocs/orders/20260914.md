---
kind: snapshot
status: active
canonical: AGENTS.md
last_verified: 2026-09-14
---

# 2026-09-14

| 상태 | 일 |
|---|---|
| 결정 | 지식 카드를 사람이 쓰는 대신 **AI 가 갱신**한다. 처음엔 제안, 수락 기록이 쌓이면 자동 — 작업지시자 결정 |
| 결정 | 연혁 이식에서 날짜가 다르면 **상세 시트(지원사업·수상·학회)가 정본** |
| 완료 | 카드 제안 구조 — `CardProposal` 모델 · `lib/card-proposals.ts` · 제안 화면 · API · 업무 완료 트리거 2곳 |
| 완료 | 수락률 기반 자동 전환 — 최근 50건 중 판단 20건 이상 · 수락률 80% 이상이면 자동. 되돌리면 거절로 세어 스스로 꺼진다 |
| 완료 | 비밀정보 차단 — 계정·비밀번호·키는 카드에 옮기지 않는다 (9-07에 평문 비밀번호가 채팅에 실린 사례) |
| 완료 | 실측 — 실제 업무(애드힐 로고 23메시지)로 제안 생성 4.2초 → 화면 수락 → 카드·버전·임베딩·활동로그 생성 → 되돌리기까지 확인 |
| 발견 | 되돌릴 때 카드를 지우면 제안까지 cascade 로 사라져 판단 기록을 잃었다 → 관계를 SetNull 로, 제안을 먼저 떼고 지운다 |
| 발견 | e2e 시나리오가 만든 `[MMDDHHmm]` 업무가 씨앗 배치 목록을 채운다 → 배치에서 제외 |
| 완료 | Notion 「옴니스(전사적 자원관리 DB)」 추출 — omnis-local MCP 설정의 토큰으로 API 직접. 목차 8행 + 하위 DB(시장기업 41 · 직원 11) + 서명 8 · 로고 9 · 조직도 3. 보유 IP 하위 DB 는 통합 미공유로 못 읽음. `~/work/omnis-import/notion/` (저장소 밖) |
| 발견 | omnis-local 의 Notion MCP 설정이 이름 `npx`·명령 `-y` 로 밀려 연결 실패 |
| 결정 | 새 모델 · HADD DB 에서 Context 로 모아 보기 · 서명 복사 · CRM 세금계산서 업로드 · **옴니스가 원본** · 인력 정정 — 설계 `mydocs/plans/2026-09-14-company-context.md` |
| 발견 | **결산 매출** 2024 3,600,800 · **2025 307,481,423** (용역 272.7백만 + 제품 34.8백만). 노션은 2025 매출을 제품만(34.75백만), 자산·자본은 2024 값·부채는 2025 값으로 섞어 적었다 |
| 발견 | 세금계산서 13장 시험 파서 — 글자 PDF 12장 검산 전부 일치, 이미지 PDF 1장(Print To PDF) 판독 불가. 세금계산서 합과 결산 매출 차이 2024 800원 · 2025 2,600원 |
| 발견 | NAS `docs/processed/*.md` 변환본이 영사이언스 세금계산서의 공급자·공급받는자를 뒤바꿔 적음 — 원본 PDF 로만 읽는다 |
| 발견 | DB 는 이미 외래키 13,957개로 그래프 모양 · 벡터 이웃 23ms(인덱스 없음) · 의미 간선은 과밀(0.75 이상 500/500) |
| 미착수 | 씨앗 배치 실행(`scripts/seed-card-proposals.ts --apply`) · 주간 배치 트리거 · 카테고리 교체 |
| 결정 | 매출 설계 확정 · 윤훈 CAO / 김경훈 CMO · 비재직 4명은 최소 정보만 · 그래프 C 는 LLM 없음(GraphRAG 아님) 설명 |
| 완료 | 별도 워크스페이스 `~/work/omnis-context` (`feat/company-context`, ai-maintained-cards 위) · 로컬 DB `omnis_context` 복제 · 온보딩(`omnis-local` · :3000 · DB `omnis`)과 분리 |
| 완료 | 스키마 8표 · 마이그레이션 `20260914030000_company_context` (DROP 0) — `omnis_context` 에만 적용 |
| 완료 | 이식 `omnis_context` — 연혁·실적 170 (지원금 합 847,700,000 = 엑셀 합계행) · 인력 11 (재직 7, 서명 4 · 직인 1 NAS) · 회사 1 · 연도 4 (확정 2 · 계획 2) · 시장기업 41 · 세금계산서 13 |
| 완료 | 세금계산서 13장 — 기관 연결 13 (한국뇌연구원 3장은 「부설 뒤 이름」 으로 바로잡음) · 견적 연결 9 · 새 기관 영사이언스 · 사업자번호 6곳 채움. 2025 공급가액 합 307,478,823 (제품 34,751,550 · 용역 272,727,273) — 결산과 차이 2,600원 |
| 발견 | 비전 판독이 이식 중에만 400 — pdf.js 가 받은 버퍼를 워커로 넘기며 비워(detach) 빈 파일이 Gemini 로 갔다. 글자 판독에 복사본을 넘겨 해결 |
| 발견 | 2025-03-05 가천대학교 산학협력단 세금계산서와 합계가 맞는 견적이 「가천대 의과대학」(HADD250305-005) 에 있다 — 자동으로 잇지 않고 표시만. 같은 곳인지 사람 확인 필요 |
| 완료 | HADD DB 회사 Context — 타일 4 · `/omnis/company` (확정 · 잠정 · 계획 분리, 결산-세금계산서 차이 표시) · `/omnis/records` · `/omnis/market` · `/omnis/staff` (관리자, 서명·직인 복사/파일 받기, 꺼낼 때마다 활동 로그). 「새 카드」 버튼 뺌 |
| 완료 | CRM 세금계산서 — `/crm/invoices` 목록 · 올리기(미리보기 → 확인 → 저장, AI 판독은 원본 대조 체크 필수, 확정 때 서버가 다시 읽고 승인번호·합계 대조) · 원본 열기. 저장 경로를 `lib/tax-invoice-save.ts` 로 모아 이식 스크립트와 같이 쓴다 |
| 완료 | Context 그래프 `/omnis/context` — 대상 하나를 가운데 두고 실선(외래키) · 점선(임베딩 유사도 0.7 이상) 한 단계, 누르면 옮겨 감 · 옆에 AI 가 읽는 조각. LLM 호출 없음. 이웃 계산 업무 47ms · 기관 15ms (omnis_context) |
| 발견 | 벡터 이웃에 채팅 조각을 넣으면 이웃 40개가 전부 채팅이다 → 채팅은 이웃 후보에서 빼고 업무 스레드 조각으로 패널에만. 같은 종류(업무↔업무)는 다른 프로젝트 것만 2개 |
| 발견 | 새 모델(연혁 · 세금계산서 · 기관 · 인력)은 임베딩이 없어 점선이 없다. 인력은 개인정보라 앞으로도 넣지 않는다 |
| 완료 | 회사 도구 6개 (`lib/company-tools.ts`, 옴니스 질문 · MCP 공용) — `company_profile` (확정·잠정·계획 재무) · `list_company_records` · `list_tax_invoices` · `list_staff` · `list_market_companies` · `get_context`. 직접 호출 10건 모두 2~36ms, 결과에 연락처·이메일·생년월일 없음 |
| 결정 | 인력 도구는 이름·소속·직급·역할·담당·학력·4대보험만. 연락처·생년월일·과학기술인번호·서명은 도구로 내보내지 않는다 (모델 공급자 · 외부 MCP 커넥터로 나가기 때문) |
| 해결 | Gemini **429 월 지출 한도 초과**로 멈췄던 회사 질문 평가 — 작업지시자가 AI Studio 한도를 올린 뒤 재실행 |
| 완료 | 회사 질문 7건 평가 (`omnis_context`, 3.8-flash low · route, dryRun) — 근거 **15/15**, 도구 선택 7/7(company_profile · list_tax_invoices · list_company_records · find_org · list_staff), 연락처·생년월일 요청은 거절하고 인력 화면 안내. 문항당 2.0~4.9초 · 7건 합계 약 46원 |
| 완료 | `feat/company-context` 에 최신 main 병합 (온보딩 · 답변 렌더 · 스레드 · MCP #10) |
| 완료 | 회사 정보 편집 — `/omnis/company` 에서 관리자만: 기본정보 편집 창 · 연도별 재무 추가/수정/삭제(두 번 눌러 삭제). 입력 규칙 `lib/schemas/company.ts` 를 폼과 API 가 같이 쓴다(사업자번호 · 법인번호 형식, 금액 쉼표·음수, 잠정은 저장 불가). 저장마다 활동 기록에 바뀐 칸 이름만(값은 남기지 않음) |
| 완료 | 편집 점검 (`omnis_context` · 브라우저) — 홈페이지 한 칸 수정 → 화면 반영 · 기록 `changed: [homepage]` · 잘못된 사업자번호는 저장 안 됨 · 2028 계획 추가 → 같은 줄 재추가 막힘 → 수정 → 삭제(기록 created → updated → deleted) · 제품+용역 차이 안내 · 390px 창 가로 넘침 0 · 일반 사용자는 편집 버튼 0, API 403. 바꾼 값·기록은 되돌림 |
| 미착수 | 연혁 · 인력 · 시장기업 편집 화면 · 운영 반영(스키마 변경 · 운영 DB 이식은 작업지시자 판단) |
| 전체 E2E 45개 통과, 사용자 요청으로 PR·배포 진행 | Hub 디자인을 참고한 Omnis 자동 재생 온보딩 8장면 + 실제 화면 스포트라이트 + 계정별 최초 안내 + 프로필의 다시 보기·MCP 등록 메뉴. 계획: `mydocs/plans/archives/2026-09-14-omnis-onboarding.md` |
| 발견 | 옴니스 AI 답변 · 주간보고 · 지식 카드 섹션이 `prose prose-sm` 을 쓰는데 `@tailwindcss/typography` 가 설치돼 있지 않아 아무 효과가 없었다 — 목록 글머리 · 들여쓰기 · 문단 간격 · 제목 크기가 사라진 채로 보임 (작업지시자 스크린샷) |
| 완료 | `feat/thread-attach-mention` — 업무 스레드 입력창을 채팅 입력창(MessageInput)으로 교체: 파일 첨부(붙여넣기·끌어놓기·+) · @사람 · #업무 자동완성(`/업무` 명령만 제외). 스레드 목록에 첨부 이미지·파일 · #/@ 링크 · 새 메시지로 스크롤 |
| 완료 | 완료된 업무 언급 — # 자동완성에 진행 중 5개 뒤로 완료 업무를 「완료 ·」 표시와 함께. 채팅에서 완료 업무를 #멘션하면 **참조만**(메시지·파일 연결, AI 재구성·상태 변경·알림 없음). 스레드 안의 다른 업무 #멘션은 그 업무로 옮겨 가지 않고 스레드에 남음 |
| 발견 | 완료 업무를 #멘션하면 AI 재구성이 `status !== IN_PROGRESS` 를 '진행 중'으로 전진시켜 완료가 풀렸다 (lib/chat-post) |
| 완료 | 워크스페이스 완료 업무 기본 숨김 + 「완료 보기 · N」 토글(브라우저에 기억). 로컬 실측: 기본 업무 노드 469 = DB 진행 469, 보기 616 = 469+147, 새로고침 뒤 유지 |
| 완료 | `fix/omnis-answer-render` — `.omnis-md` 마크다운 스타일(globals.css, components 레이어) 로 4곳 교체 · 각주 `[11, 12]` · `[7-9]` 도 배지로, 제목·표 칸에도 적용 · 출원번호 줄바꿈 방지 · 줄바꿈 한 번을 hard break 로 · 답변 규칙에 표·소제목·각주 위치 지침. 예시 답변 2건으로 밝은/어두운 테마 · 390px 확인(가로 넘침 0, 괄호로 남은 각주 0) 뒤 예시 삭제 |

- 추가 요청 반영: 입력창/전송 메시지 확대, 자동+좌우 탭 스토리 탐색. 온보딩 11개 포함 전체 E2E 45개 통과. 사용자 PR·머지·배포 승인.
