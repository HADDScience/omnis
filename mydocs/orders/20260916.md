---
kind: snapshot
status: active
canonical: mydocs/orders/20260916.md
last_verified: 2026-09-16
---

# 2026-09-16 작업

| 상태 | 내용 |
|---|---|
| 등록 | 채팅 메시지 **수정 · 삭제 · 답장** — `feat/chat-message-actions` · 계획 `plans/2026-09-16-chat-message-actions.md` |
| 등록 | 업무의 **담당자 · 지시자 변경** — `feat/task-people-edit` · 계획 `plans/2026-09-16-task-people-edit.md` |
| 확인 | 지시자 방향은 로직 정상 — 지시자는 `/업무` 를 친 사람(`app/api/tasks/route.ts:182`), 담당자는 모달 선택. 작업지시자 입력이 반대였다 |
| 확인 | 운영에 잘못 들어간 업무는 **화면에서 고친다** — 담당자 · 지시자 변경 기능 배포 후. 운영 DB 직접 수정은 하지 않는다 |
| 완료 | **담당자 · 지시자 변경** — 로컬 dev 실측: 담당자 정우창→노혜린 · 지시자 김아리→허채정, 새 담당자에게만 `accept_task` 알림, 활동 기록 metadata 에 바뀐 사람. `working/2026-09-16-task-people-edit.md` |
| 완료 | **채팅 수정 · 삭제 · 답장** — 답장 인용 · 「수정됨」 · 「삭제된 메시지입니다」 왕복 확인. 스레드에서 고치면 재구성이 다시 돈다(`task.rebuild.finished · complete`). `working/2026-09-16-chat-message-actions.md` |
| 고침 | 고친 글이 화면에 반영되지 않았다 — 폴링은 `after=` 뒤 새 글만 가져온다. 응답으로 받은 글을 그 자리에서 갈아 끼운다 |
| 대기 | 두 브랜치 PR · 채팅 쪽은 **운영 DB 마이그레이션을 머지 전에** 적용해야 한다 — 작업지시자 승인 |
| 답변 | AI 재구성은 순차가 아니다 — 메시지마다 `after()` 로 각자 돌고, 같은 업무면 재구성이 **그 업무의 모든 메시지를 다시 읽는다**. 늦게 끝난 옛 결과는 `superseded` 로 버린다(`lib/chat-post.ts:228`). 큐가 아니라 "마지막이 이긴다" |
| 완료 | **HADD DB 랜딩 — 히어로를 회사 Context 에 넘긴다** — 카드 0건인데 히어로·검색·칩이 전부 카드를 향하고 있었다. 타일을 앞으로, 0건일 때 칩 줄과 3열은 감춘다. `feat/hadd-db-landing-context-first` · `working/2026-09-16-hadd-db-landing-context-first.md` |
| 고침 | 로컬 dev 500 — Prisma 클라이언트 재생성 + 로컬 DB 에 `20260916000000_chat_message_actions` 적용(운영 아님) |
| 남음 | 검색 대상을 회사 자료(연혁 170건 · 시장기업 41곳)·NAS 까지 넓히는 A안 — `EmbeddingSource` 확장 + 백필 + 검색 결과 화면 |
