---
status: working
owner: 정우창
updated: 2026-09-15
scope: 채팅 · 업무 스레드 메시지 목록 가독성
---

# 2026-09-15 작업 기록

| 구분 | 내용 |
|---|---|
| 완료 | PR #14 머지(477de5e, 작업지시자 요청) → 운영 `omnis-hadd` · 데모 `omnis` 둘 다 Ready. 데모 빌드에서 `prisma migrate deploy` 통과. 데모 실사이트 팀장 로그인 — 회사 · 연혁 · 인력 · 세금계산서 · 견적에 데모 데이터, 파일 업로드 403 안내. 운영 `/omnis/api/files` 비로그인 401(데모 차단 아님) |
| 발견 | 업무 스레드(오른쪽 380px 패널)가 읽기 어렵다 (작업지시자 스크린샷) — 같은 사람이 이어 보내도 줄마다 이름 · "9월 14일 오후 02:08"(고정폭 글꼴) 반복, 업무 생성 사건이 사람 메시지처럼 보임, 파일은 이름 · 크기 글자뿐, 한 글자 옅은 아바타, 빈 줄이 그대로 큰 틈. 채팅 패널(말풍선)도 묶기 · 날짜 구분선 없음 |
| 조사 | Discord — 같은 작성자 연속 메시지를 첫 메시지부터 7~8분 안이면 머리 한 번 · Slack — 묶음 안 시간은 hover, 시간에 hover 하면 전체 시각 · 한글 본문 — `word-break: keep-all`, 행간 150% 이상(모바일 155%) · 첨부는 형식 아이콘 카드 |
| 결정 | 적용 8가지: 연속 메시지 묶기(5분) · 날짜 구분선(오늘/어제/9월 14일 (월)) · 짧은 시간 "오후 2:08" + hover 전체 시각, 고정폭 대신 tabular-nums · 사건(생성/완료/확인 대기) 옅은 한 줄 · 이름별 색 아바타(한글 이름 두 글자) · 본문 13px/1.6 · keep-all · 빈 줄 여러 개는 하나로 · 파일 형식 카드 · @/# 칩. 뺀 것: 반응 · 안 읽음 구분선 · hover 도구막대(해당 기능이 없다) |
| 완료 | `feat/chat-readability` — `lib/chat-layout.ts`(묶기 · 날짜 · 시간 · 빈 줄 · 아바타 색, 순수 함수) · `components/chat/message-parts.tsx`(DayDivider · AuthorAvatar · MessageTime · EventRow) · 업무 스레드 · 채팅 패널 적용 · `MessageFiles` 형식 카드 · `MessageContent` 멘션 칩 · 묶음 안 같은 업무 태그 반복 제거 · 목록에 `role="log"` |
| 해결 | 묶음 안 줄 본문이 5px 왼쪽으로 어긋남 — 여백 칸이 rem(`w-7`), 아바타가 px(28) 이라 html 배율에서 달라짐 → px 로 맞춤 |
| 해결 | 아바타 색이 팀장 · 부팀장 · 사원1 모두 같은 색 — `h*31+c` 가 한글 코드포인트에서 8 나머지가 몰림 → FNV-1a. 바꾸는 중 부호 있는 32비트로 나눠 색이 undefined 가 되는 버그를 점검에서 잡아 `>>> 0` 으로 고침(임의 이름 5,000개 undefined 0) |
| 완료 | 점검 — 배치 규칙 단정 11개 통과(묶음 경계 · 날짜 · 오전 12시/오후 12시 · 빈 줄 · 아바타 글자) · tsc 통과 · eslint 오류 0. 새 로컬 DB `omnis_chat_check`(데모 시드 + 스크린샷과 같은 모양의 스레드) 브라우저: 날짜 구분선 어제/오늘 · 사건 줄 · 파일 카드 "PPT · 105KB" · 고정폭 글꼴 0 · 본문 왼쪽 x 6줄 모두 같음 · 채팅 패널 묶음 5개에 업무 태그 5개 · 다크 모드 · 390px 가로 넘침 0 · 콘솔 · 서버 로그 오류 0. 점검 서버 · DB 정리 |
| 결정 | 채팅 보내기 효과 추가 · 구조화 로딩 개선은 방식부터 정한다 (작업지시자) |
| 완료 | 보내기 효과 — 채팅 패널: 보내는 중 말풍선이 아래에서 떠오르며 옅게 + 「보내는 중…」, 서버 응답이 오면 그 자리에서 확정(`_settled`, 다시 움직이지 않음) · 새 메시지는 부드럽게 따라 내려감. 업무 스레드: 서버가 AI 재구성까지 끝낸 뒤 응답(실측 8~13초)해 그동안 글이 안 보였다 → 누르는 즉시 「보내는 중 · 옴니스가 업무를 갱신하고 있어요」 줄, 목록 길이가 바뀌면 사라짐. 처음 불러온 · 이전 메시지는 움직이지 않고, 움직임 줄이기 설정이면 멈춤 |
| 완료 | 보내기 점검(로컬, 기록기로 DOM 변화 측정) — 채팅: 보내는 중 줄 떠오름 · 반투명 → 79ms 뒤 시각으로 확정 · 다시 떠오르지 않음 · 맨 아래. 스레드: 보내는 중 줄 떠오름 → 335ms 뒤 실제 메시지로 바뀜 · 줄 남지 않음 · 맨 아래. tsc 통과 · eslint 오류 0 |
| 발견 | 구조화 대기 — 스레드 답장은 `postChatMessage` 가 `rebuildTask` 를 기다린 뒤 응답(8~13초, 입력창 가림), `/업무` 구조화는 2~4초 버튼 스피너 (mydocs/working/2026-09-08-scenario-e2e.md 실측) |
