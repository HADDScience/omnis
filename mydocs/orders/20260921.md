---
kind: snapshot
status: active
canonical: mydocs/orders/20260921.md
last_verified: 2026-09-21
---

# 2026-09-21 작업

회사 데이터가 Gemini 로 나가는 것에 대한 우려에서 시작해, 근거 확인 → 키 교체 →
밖으로 나가는 것을 줄이는 순으로 갔다. 코드 변경은 아직 커밋하지 않았다.

| 상태 | 내용 |
|---|---|
| 확인 | 무엇이 Google 로 나가는지 코드로 특정 — 임베딩(업무카드·업무·주간보고·채팅·IP건 **전문**, 채팅은 등록 즉시) · 세금계산서 **원본 파일 전체** · 업무 지시 대화 원문 · 질의 컨텍스트 · 기사 본문. `tech/gemini-data-handling.md` |
| 확인 | Gemini 프로젝트(`gen-lang-client-0493103520`)가 **Tier 1 · 선불** — 유료 티어라 프롬프트·응답이 학습에 쓰이지 않는다. 결제 계정을 켜면 **무료 할당량 호출도** 데이터 사용 측면에서 유료로 간주된다(약관 본문) |
| 확인 | Google 데이터 처리 부록 v10(2026-05-07)의 처리자 서비스 목록에 **Gemini API Paid Services** 등재 — 처리위탁 관계가 성립한다. 무료 티어는 목록에 없다(= 수탁자가 아니라 자기 목적 처리 주체) |
| 확인 | 학습 코퍼스에서 **기업 기밀이 추출된 공개 사례는 없다.** 추출에 성공한 사례(Copilot 유효 비밀값 2,702건)는 전부 공개 저장소에 중복 수록돼 학습에 들어간 값이다. 암기 확률은 중복 횟수에 좌우된다 |
| 결정 | 위험 순위 — 1 직원 개인 AI 계정 · 2 벡터 DB·로컬 운영 스냅샷 · 3 간접 프롬프트 인젝션 · 4 RAG 권한 우회 · 5 학습 코퍼스(유료 티어라 해당 없음). **실제 위험은 Google 쪽이 아니라 1·2** |
| 완료 | Gemini API 키 교체 — 새 키 발급 → 로컬 `.env` · omnis-hadd production(sensitive 타입) → 재배포 `70mxxqij5` Ready · `omnis-hadd.vercel.app` HTTP 200. **옛 키는 아직 AI Studio 에 살아 있다** |
| 사고 | **옛 배포를 재배포해 운영이 나흘 전 코드로 덮였다.** 새 Gemini 키를 적용하려고 11:29 에 `vercel redeploy` 를 걸었는데, 대상이 9/17 커밋의 배포였다. 재배포는 소스를 그대로 다시 굽고 환경변수만 새로 입힌다 — 키는 반영됐지만 허브 등록(`hub-com`)·호환 rewrite 가 빠져 허브 로그인이 막혔다. 루트 경로가 200 이라 그때는 정상으로 보고했다. 복구와 경위는 `working/2026-09-21-pnpm-and-stale-redeploy.md` |
| 고침 | `jose` 가 `package.json` 에 없는데 `lib/sso.ts:21` 이 직접 import — npm 호이스팅으로 우연히 돌던 phantom 의존성. pnpm 으로 빌드된 배포가 `Cannot find module 'jose'` 로 실패했다. **같은 문제를 다른 세션이 PR #51 에서 더 넓게 풀었다** — pnpm 으로 통일하고 `jose`·`playwright`·`sharp` 를 직접 의존성으로 올렸다. 이쪽 임시 수정(`jose ^6.0.0`)은 버렸다 |
| 완료 | 로컬 스냅샷 마스킹 — `scripts/mask-snapshot.sql` 신규, `db:snapshot` 의 **기본값이 마스킹**이고 `--raw` 가 원문. 실데이터 rollback 검증: 16개 UPDATE 전부 성공 · 0.84초 · 오류 0. 채팅 본문 235자 → 235자(길이 보존), 벡터 전부 비움 |
| 완료 | 보안 검토 보고서 작성 — Claude Docs. 약관·가격표·처리자 목록 캡쳐 4장 포함 |
| 확인 | Claude Docs 는 **공개 링크 공유가 안 된다**(「Live documents can't be shared publicly yet.」). 이메일 초대 또는 Export 뿐 |

## 남긴 것

- 로컬 DB 에 운영 원문이 그대로 있다 — 업무 594 · 채팅 12,774 · 색인 8,010. 마스킹은 만들어 뒀지만 **아직 적용하지 않았다**
- 옛 Gemini 키 삭제 · omnis-demo(공개 데모) 키 정리
- `pnpm-lock.yaml`·`pnpm-workspace.yaml` 이 커밋되지 않은 채로 남아 있다 → **PR #51 에서 pnpm 으로 통일돼 해결됐다**
- 후속 과제: 검색 계층 권한 필터 · 세금계산서 원본 전송 축소 · `gemini-embedding-001` 레거시 확인
