---
kind: snapshot
status: active
canonical: mydocs/orders/20260922.md
last_verified: 2026-09-22
---

# 2026-09-22 작업

어제([20260921](20260921.md))는 Gemini 데이터 취급을 확인하고 키 교체·마스킹까지 했다.
코드는 아직 커밋 전이고, 사람이 정해야 할 것이 셋 남아 있다.

## 먼저 정할 것

| 상태 | 내용 |
|---|---|
| 결정 | **로컬 DB 에 마스킹을 적용할지.** 지금 이 컴퓨터에 운영 원문이 있다 — 업무 594 · 채팅 12,774 · 색인 8,010. 적용해도 `pnpm db:snapshot --raw` 로 언제든 원문을 되받을 수 있다 |
| 결정 | **보안 검토 보고서를 어떻게 전달할지.** Claude Docs 는 공개 링크 공유가 안 된다 — Export(파일 첨부, 계정 불필요) 또는 이메일 초대(Claude 계정 필요, 이후 댓글로 논의 가능) |

## 할 일

| 상태 | 내용 |
|---|---|
| 완료 | 어제 변경분을 `feat/snapshot-masking` 으로 커밋 → PR #53. `jose` 임시 수정은 PR #51 이 더 넓게 풀어 버렸고, 문서의 명령은 pnpm 으로 맞췄다 |
| 대기 | **옛 Gemini 키 삭제** — AI Studio 에 아직 살아 있다. 운영은 새 키로 떠 있으므로(`70mxxqij5`) 지워도 되지만, 그 전에 Omnis 에서 채팅 한 줄 → 업무 카드 생성으로 새 키가 실제로 동작하는지 확인한다. omnis-demo 가 쓰는 키는 남긴다 |
| 대기 | omnis-demo 키 정리 — 어느 키를 쓰는지 모르면 데모 키도 새로 발급해 갈아끼워야 나머지를 마음 놓고 지울 수 있다 |

## 진행 중 (다른 세션)

| 상태 | 내용 |
|---|---|
| 확인 | `scripts/tmp-website-{probe,token,media-add}.ts` 3개가 오늘 09:20~09:23에 생겼다(미커밋). 홈페이지 기사의 `position`·`category`·미디어를 다루는 일회용 스크립트로 보인다. 일이 끝나면 지우거나, 남길 것이면 `tmp-` 를 떼고 용도를 주석에 적는다 |

## 후속 과제 (급하지 않음)

- **검색 계층 권한 필터** — `retrieveContext` SQL 에 사용자 필터가 없다. 비공개 방·관리자 전용 카드를 넣기 **전에** 넣어야 한다. 나중에 넣으면 이미 늦다. `tech/gemini-data-handling.md`
- **세금계산서 원본 전송 축소** — 지금은 PDF 전체를 base64 로 보낸다. 필요한 필드만 뽑아 보내면 밖으로 나가는 내용이 크게 준다
- **`gemini-embedding-001` 레거시** — 현재 Google 가격표에 없고 `gemini-embedding-2` 로 대체됐다. 지원 종료 일정 확인 필요. 보안이 아니라 운영 문제다
